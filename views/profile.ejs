<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connectify</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-700 text-shite p-10">

        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>

        <!-- Heading -->
        <h1
            class="mb-4 text-4xl font-extrabold leading-none tracking-tight text-gray-900 md:text-5xl lg:text-6xl dark:text-white text-center pb-4">
            Hello <mark class="px-2 text-white bg-blue-600 rounded dark:bg-blue-500">
                <%= user.name %>
            </mark>
        </h1>
        <h5 class="text-white mb-5">You can create a new POST</h5>
        <form action="/post" method="post">
            <textarea
                class="w-1/3 p-3 block text-white resize-none bg-transparent border-2 border-zinc-800 rounded-md outline-none"
                placeholder="What's on your mind" name="content"></textarea>
            <input class="px-3 py-2 w-30 bg-blue-500 block rounded-md mt-2" type="submit" value="Create new post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">

                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 mb-4 rounded-md px-5 border-[1px] bg-zinc-800 border-zinc-700">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %>
                        </h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small class="inline-block mt-2">
                            <%= post.likes.lenght %>
                        </small>

                        <div class="btns flex gap-4 mt-5">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                    <% }) %>

            </div>
        </div>


    </div>




</body>

</html>