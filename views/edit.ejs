<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connectify</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-700 text-shite p-10">

        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>

        <h5 class="text-white mb-5">Edit your POST</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea
                class="w-1/3 p-3 block text-white resize-none bg-transparent border-2 border-zinc-800 rounded-md outline-none"
                placeholder="What's on your mind" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 w-30 bg-yellow-500 text-black block rounded-md mt-2" type="submit"
                value="Update Post">
        </form>

    </div>




</body>

</html>